
##
# Application name, which should also be exactly
# similar to the name of your config file
##
spring.application.name=ms-sdp-header-decryptor

##
# The location from which the service will fetch its configuration
##
spring.cloud.config.uri=http://10.184.38.86:31437

##
# The profile under which to run the application,
# also the type of config to fetch from the server
##
spring.profiles.active=development

##
# This will expose the refresh endpoint to allow change of configuration during runtime
##
management.endpoints.web.enabled-by-default=false
management.endpoints.web.exposure.include=refresh,health
